# EOS Growth Report Site

Sanitized public web version of the EOS Erleben growth, traffic and funnel audit.

The site contains aggregate metrics only. Credentials, visitor-level Umami data, personal information and individual Supabase request identifiers are intentionally excluded.

Open `index.html` locally or publish this directory with any static hosting provider.
